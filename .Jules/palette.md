## 2026-06-16 - Icon-Only Button Accessibility
**Learning:** Icon-only buttons without `aria-label` attributes are inaccessible to screen reader users because the visual icon provides no programmatic context.
**Action:** Always add descriptive `aria-label` attributes and consider adding `title` tooltips for sighted users to clarify the action of icon-only buttons.
